node_modules/
dist/
build/
coverage/
.DS_Store
*.log
.env*
!.env.example
# Черновые скриншоты/скрипты для ручной проверки, не часть приложения.
scratch/
# Локальная копия docker-compose.yml больше не отслеживается — версия на
# GitHub считается канонической и не должна перезаписываться будущими
# локальными правками (см. deploy-документацию по docker-compose.override.yml).
docker-compose.yml
