/**
 * Реестр 5 новых корпоративных тестов — только математика (id пунктов,
 * группировка в подшкалы, диапазон шкалы). Порядок/текст самих вопросов —
 * во frontend i18n (t.tests.<key>.questions[]), в ТОМ ЖЕ порядке, что
 * question.id здесь (id — это 1-based индекс вопроса в массиве вопросов
 * теста; см. комментарий в apps/web/src/i18n/ru.ts рядом с t.tests).
 *
 * Названия тестов намеренно НЕ совпадают с международными брендированными
 * названиями методик (eNPS/MBI/TIS-6/WHO-5/Edmondson) — по требованию
 * заказчика; вопросы переформулированы, а не переведены дословно.
 */
import type { TestDefinition, TestKey } from './types.js';

export const TEST_DEFINITIONS: Record<TestKey, TestDefinition> = {
  // Тест на лояльность сотрудников (по мотивам eNPS) — 1 рейтинг 0-10 +
  // необязательный открытый комментарий. Компанейский агрегат считается
  // отдельно (доля "промоутеров" 9-10 минус доля "критиков" 0-6) — см.
  // corporateAggregation.ts, а не headline-формулой этого движка (которая
  // даёт только индивидуальный балл 0-100 для личного отчёта сотрудника).
  loyalty: {
    key: 'loyalty',
    scaleMin: 0,
    scaleMax: 10,
    questions: [{ id: 1 }],
    hasOpenText: true,
  },

  // Тест на выгорание (по мотивам 3-факторной структуры MBI: истощение /
  // цинизм-отстранённость / снижение результативности), 9 пунктов вместо
  // оригинальных 22 — по требованию "версия с меньшим числом вопросов".
  // Все пункты сформулированы так, что высокий балл = больше признаков
  // выгорания по этой подшкале (см. комментарий в types.ts про
  // reverseScored) — включая подшкалу результативности: пункты про
  // СНИЖЕНИЕ результативности, а не про эффективность напрямую.
  burnout: {
    key: 'burnout',
    scaleMin: 1,
    scaleMax: 5,
    questions: [1, 2, 3, 4, 5, 6, 7, 8, 9].map((id) => ({ id })),
    subscales: [
      { key: 'exhaustion', questionIds: [1, 2, 3] },
      { key: 'cynicism', questionIds: [4, 5, 6] },
      { key: 'accomplishment', questionIds: [7, 8, 9] },
    ],
  },

  // Тест риска увольнения (по мотивам TIS-6) — 6 пунктов, один общий балл.
  turnover: {
    key: 'turnover',
    scaleMin: 1,
    scaleMax: 5,
    questions: [1, 2, 3, 4, 5, 6].map((id) => ({ id })),
  },

  // Тест общего благополучия (по мотивам WHO-5) — 5 пунктов, один общий
  // балл, положительная формулировка (высокий балл = выше благополучие).
  wellbeing: {
    key: 'wellbeing',
    scaleMin: 1,
    scaleMax: 5,
    questions: [1, 2, 3, 4, 5].map((id) => ({ id })),
  },

  // Тест психологической безопасности (по мотивам шкалы Эдмондсон) —
  // 7 пунктов, один общий балл.
  psychSafety: {
    key: 'psychSafety',
    scaleMin: 1,
    scaleMax: 5,
    questions: [1, 2, 3, 4, 5, 6, 7].map((id) => ({ id })),
  },
};
